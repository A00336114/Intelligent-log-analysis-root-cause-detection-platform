input {
  tcp {
    port => 5000
    codec => json
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "banking-logs-%{+YYYY.MM.dd}"
  }

  tcp {
    host => "splunk"
    port => 10514
    codec => json_lines
  }

  stdout {
    codec => rubydebug
  }
}
