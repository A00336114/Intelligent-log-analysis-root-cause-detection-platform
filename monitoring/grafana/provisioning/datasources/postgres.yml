apiVersion: 1

datasources:
  - name: PostgreSQL
    uid: postgresql
    type: postgres
    access: proxy
    url: postgres:5432
    user: admin
    secureJsonData:
      password: password
    jsonData:
      database: platform_db
      sslmode: disable
      encrypt: disable
